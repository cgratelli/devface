<%= form_for(@user_rol) do |f| %>
  <% if @user_rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_rol.errors.count, "error") %> prohibited this user_rol from being saved:</h2>

      <ul>
      <% @user_rol.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
